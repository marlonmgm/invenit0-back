//var email = require('some-email-lib')

module.exports = function(agenda) {

  agenda.define('email.registro', (job, done) => {
    done();
  });

  agenda.define('email.bienvenida', (job, done) => {

    done();

  });

};
